#include "libft.h"

char				*ft_strsub(const char *s, unsigned int start, size_t len)
{
     /*
        man page req:
            - Allocates (with malloc(3)) and returns a “fresh” substring from the string given as argument. 
            - The substring begins at indexstart and is of size len. 
            - If start and len aren’t refer- ing to a valid substring, the behavior is undefined. 
            - If the allocation fails, the function returns NULL.
      */

}